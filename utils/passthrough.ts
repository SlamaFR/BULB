import type { PrimeVuePTOptions } from 'primevue/config'

const Passthrough: PrimeVuePTOptions = {
  dialog: {
    root: {
      class: 'm-4',
    },
  },
  confirmdialog: {
    root: {
      class: 'max-w-40em m-4',
    },
  },
}

export default Passthrough
